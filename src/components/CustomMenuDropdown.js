import { Button } from '@material-ui/core';
import React from 'react';

const CustomMenuDropdown = ({name}) => {
    return (
        <Button>{name}</Button>
    )
}

export default CustomMenuDropdown;